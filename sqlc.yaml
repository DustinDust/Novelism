version: "2"
sql:
  - engine: "postgresql"
    queries: internals/data/query.sql
    schema: migrations
    gen:
      go:
        package: "data"
        out: "internals/data"
        sql_package: "pgx/v5"
        emit_json_tags: true
        emit_db_tags: true
        emit_empty_slices: true
